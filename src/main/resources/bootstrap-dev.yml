server:
  port: 8080
Spring:
  cloud:
    gateway:
      routes:
        - id:
  kafka:
    #    a default kafka instance[http://kafka.apache.org/quickstart] in local
    bootstrap-servers: localhost:9092